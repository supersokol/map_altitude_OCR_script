# map_altitude_OCR_script
Script for processing cartographic data: detects altitude marks on .tif images using OCR and outputs table with global coordinates based on data from .tfw files for further use.
